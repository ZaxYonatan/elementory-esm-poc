const processRequest = async (filePath, methodName, args) => {
    const module = await import(filePath);
    const fn = module[methodName];
    return fn(...args)
}

module.exports = {
    processRequest
}